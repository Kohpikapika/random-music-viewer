# AGENTS.md

このファイルは、このリポジトリで作業するエージェントが毎回最初に読む前提資料です。

## 作業開始時に必ず読むもの

1. この `AGENTS.md`
2. このリポジトリの `README.md`
3. Fetcher 側（`../yt-music-fetcher`）の `AGENTS.md` と `README.md`
4. 変更対象に関係する `package.json`、`vite.config.js`、`public/`、`src/`

README と実装が食い違う場合は、現在の実装とこのファイルに書かれた最新のプロジェクト前提を優先する。ただし、食い違いを黙って解消したり、大きな仕様変更を推測で行ったりしないこと。

## プロジェクト全体の前提

- この制作物は Fetcher と Viewer の 2 リポジトリで構成される。
- Viewer は VTuber ユニット「MYME（マイミー）」を紹介し、その音楽動画に気軽に出会える場として作られている。
- MYME のメンバーは「繭糸（マユイ）」「みちとせ（ミチトセ）」「それ故（ソレユエ）」の 3 人。
- 当初は MYME のグループチャンネルが存在しなかったため、3 人それぞれの個人チャンネルからランダムに 1 本ずつ選び、一画面で見られるサイトとして設計された。
- 現在は MYME のグループチャンネルが開設され、グループ固有の動画も投稿されている。今後の情報設計や画面構成では、個人 3 チャンネルだけを不変の前提にしない。
- 根底にある方針は、運用コストを可能な限りゼロに近づけること。実行時に YouTube API を呼ばず、静的 JSON と軽量な表示処理を使う現在の構成を尊重する。
- ポートフォリオとして技術を誇示することより、MYME の魅力が伝わること、気軽に使えること、壊れにくく長期間ほぼ放置できることを重視する。

## Viewer の責務

- Fetcher が生成した `public/music.json` を読み込み、動画を表示する。
- 手動追加データの `public/music.manual.json` が存在する場合は、自動生成データと合わせて扱う。
- 現在の画面は `channelKey` ごとに動画をまとめ、各グループからランダムに 1 本ずつ選ぶ。
- Viewer の実行時には YouTube Data API を呼ばない。
- 表示、ランダム選択、アクセシビリティ、レスポンシブ対応など、閲覧体験に関する処理を担当する。
- フロントエンドは Svelte、ルーティングは Routify を使用する。ページは `src/routes/` のディレクトリ／ファイル構成に対応させる。
- ライバーの識別色は、繭糸＝オレンジ、みちとせ＝緑、それ故＝水色を必ず使用する。
- 画面上では非公式ファンサイトであることを明瞭に示し、MYME の公式サイトや公式チャンネルと誤認される見せ方を避ける。
- ルート画面の表示タブは「ホーム／動画／ショート／繭糸／みちとせ／それ故」。タブはページ内スクロールではなく、同じ画面内の表示内容を切り替える。
- ショート判定は動画タイトルの `#shorts` を大文字・小文字を区別せずに照合する。動画タブにはショートを含めない。
- Viewer はタイトルに「受注販売」または「ライブ配信」を含む動画をすべての表示対象から除外する。
- 複数ライバーを扱う一覧では、特定の1人だけに偏らず、3人それぞれのセクションを設ける。
- 動画タブの一覧は3列固定を基本とし、各行が繭糸、みちとせ、それ故の順になるよう動画カードを交互に並べる。
- ショートは該当動画があるライバーごとの動画棚で表示する。それ故はショートを基本的に投稿していない前提とし、該当動画がない空の棚は表示しない。
- 繭糸、みちとせ、それ故の個人タブは初回から最大8件をランダム表示し、「もう一回引く」でそのライバーの動画だけを再抽選する。
- セクションの区切りは枠線を多用せず、余白とライバー識別色を中心に表現する。

データの取得・更新は Fetcher の GitHub Actions が担当する。現在は Fetcher で生成された `music.json` がこのリポジトリの `public/music.json` に commit/push され、その更新によって Viewer が再デプロイされる。

## 変更時の判断基準

- MYME の紹介サイトであるというコンセプトを、単なる汎用 YouTube ランダム再生アプリより優先する。
- グループチャンネルの扱いを追加・変更する際は、個人 3 人の動画とグループ固有動画をどう見せ分けるかを明確にする。未確認のチャンネル ID、正式表記、URL、人物情報を推測で追加しない。
- `music.json` の形式変更が必要なら、先に Fetcher の生成処理と GitHub Actions の転送経路を確認する。
- 自動生成される `public/music.json` を手作業で恒久修正しない。手動管理が必要なデータは、その目的に合う既存ファイルまたは明示的な設定へ置く。
- 常駐バックエンド、データベース、有料 API、日常的な手動更新を安易に導入しない。
- シンプルさを保ちつつ、MYME と各メンバーの魅力が伝わる情報設計を選ぶ。
- 変更後はローカル起動、JSON 読み込み、ランダム選択、空データや取得失敗時の挙動、主要画面幅での表示を変更範囲に応じて確認する。

## リポジトリ間の境界

- データの取得・整形・自動更新は Fetcher の責務。
- 表示・ランダム選択・画面デザインは Viewer の責務。
- 両方にまたがる変更では片方だけ直して完了とせず、契約となる JSON 形式とデプロイ経路まで確認する。
